import 'package:flutter/material.dart';

class JustSlide extends StatelessWidget {
  const JustSlide({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('Just Nothing'),
      ),
    );
  }
}
